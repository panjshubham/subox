import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create Business Account | ShuBox Industrial",
  description: "Register for a ShuBox business account to unlock trade pricing, bulk discounts, and direct factory rates for industrial electrical enclosures.",
};

export default function RegisterLayout({ children }: { children: React.ReactNode }) {
  return children;
}
